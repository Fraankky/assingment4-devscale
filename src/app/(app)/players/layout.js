export default function PlayerLayout({ children }) {
    return (
      <div className="bg-gradient-to-tr from-indigo-900 via-sky-300 to-lime-200 min-h-screen">
        {children}
      </div>
    );
  }
  